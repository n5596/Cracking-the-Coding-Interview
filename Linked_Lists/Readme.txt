## Chapter 2 in the Interview Questions section.

2.1 Remove duplicates in a linked list
2.2 Return kth to last node
2.3 Delete middle node
2.4 Partition list around a pivot (smaller elements are placed before pivot and other elements are placed after pivot)
2.5 Add the numbers represented by two linked lists
2.6 Check if a linked list is a palindrome
2.7 Determine if two linked lists intersect
2.8 Detect if there is a loop in the linked list
